"use client";

import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { CheckCircle2, FileBarChart, Scale, XCircle } from "lucide-react";

import { LoadingSpinner } from "@/components/loading-spinner";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { formatDate, formatINR } from "@/lib/format";
import { fetchBalanceSheet } from "./reports-api";

const today = new Date().toISOString().slice(0, 10);

function ReportSection({ title, description, lines, total }: { title: string; description: string; lines: { code: string; name: string; balance: number }[]; total: number }) {
  return <Card><CardContent className="p-0"><div className="border-b border-border p-5"><h2 className="font-semibold text-text">{title}</h2><p className="mt-1 text-xs text-text-muted">{description}</p></div>{lines.length === 0 ? <p className="p-5 text-sm text-text-muted">No posted balances in this section.</p> : <div className="divide-y divide-border">{lines.map((line) => <div key={line.code} className="flex items-center justify-between gap-4 px-5 py-3"><div><p className="text-sm font-medium text-text">{line.name}</p><p className="text-xs text-text-muted">{line.code}</p></div><p className="text-sm font-semibold text-text">{formatINR(line.balance)}</p></div>)}</div>}<div className="flex items-center justify-between border-t border-border bg-surface-muted/50 px-5 py-3"><span className="text-sm font-semibold text-text">Total {title}</span><span className="text-sm font-bold text-text">{formatINR(total)}</span></div></CardContent></Card>;
}

export function BalanceSheetPage() {
  const [asOfDate, setAsOfDate] = useState(today);
  const reportQuery = useQuery({ queryKey: ["balance-sheet", asOfDate], queryFn: () => fetchBalanceSheet(asOfDate) });
  const report = reportQuery.data;

  return <div className="space-y-6 pb-12"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary-600">Reports / Financial statements</p><h1 className="mt-2 text-2xl font-bold tracking-tight text-text sm:text-3xl">Balance Sheet</h1><p className="mt-1 max-w-2xl text-sm text-text-muted">Assets, liabilities, and capital based on posted journal entries.</p></div><div className="flex items-end gap-2"><label className="text-xs font-medium text-text-muted">As of date<input type="date" value={asOfDate} onChange={(event) => setAsOfDate(event.target.value)} className="mt-1 block rounded-lg border border-border bg-surface px-3 py-2 text-sm text-text" /></label><Button type="button" variant="outline" onClick={() => window.print()}><FileBarChart className="h-4 w-4" />Print</Button></div></div>{reportQuery.isLoading ? <div className="flex justify-center py-20"><LoadingSpinner /></div> : reportQuery.isError || !report ? <Card><CardContent className="p-10 text-center"><p className="text-sm text-red-600">Unable to load the Balance Sheet.</p><p className="mt-1 text-xs text-text-muted">Check that the accounting report API is available and try again.</p></CardContent></Card> : <><div className="grid gap-4 sm:grid-cols-3"><Card size="sm"><CardContent className="flex items-center gap-3"><div className="rounded-lg bg-blue-50 p-2.5 text-blue-600 dark:bg-blue-950/40"><Scale className="h-5 w-5" /></div><div><p className="text-xs text-text-muted">Total assets</p><p className="text-xl font-bold text-text">{formatINR(report.total_assets)}</p></div></CardContent></Card><Card size="sm"><CardContent><p className="text-xs text-text-muted">Liabilities + capital</p><p className="mt-1 text-xl font-bold text-text">{formatINR(report.total_liabilities_and_capital)}</p></CardContent></Card><Card size="sm"><CardContent><p className="text-xs text-text-muted">As of</p><p className="mt-1 text-sm font-semibold text-text">{formatDate(report.as_of_date)}</p></CardContent></Card></div><div className={`flex items-center justify-between gap-4 rounded-xl border px-4 py-3 ${report.balanced ? "border-emerald-200 bg-emerald-50 dark:border-emerald-900 dark:bg-emerald-950/30" : "border-red-200 bg-red-50 dark:border-red-900 dark:bg-red-950/30"}`}><div className="flex items-center gap-2">{report.balanced ? <CheckCircle2 className="h-5 w-5 text-emerald-600" /> : <XCircle className="h-5 w-5 text-red-600" />}<div><p className={`text-sm font-semibold ${report.balanced ? "text-emerald-700 dark:text-emerald-300" : "text-red-700 dark:text-red-300"}`}>{report.balanced ? "Balance Sheet is balanced" : "Balance Sheet is not balanced"}</p><p className="text-xs text-text-muted">Assets = Liabilities + Capital + Current Period Result</p></div></div><Badge variant={report.balanced ? "secondary" : "destructive"}>{report.balanced ? "Balanced" : "Check entries"}</Badge></div><div className="grid gap-5 xl:grid-cols-2"><ReportSection title="Assets" description="Resources owned by the business" lines={report.assets} total={report.total_assets} /><div className="space-y-5"><ReportSection title="Liabilities" description="Amounts owed by the business" lines={report.liabilities} total={report.total_liabilities} /><ReportSection title="Capital" description="Owner equity and current period result" lines={report.capital} total={report.total_capital + report.net_income} /></div></div><Card><CardContent className="flex flex-col gap-2 p-5 text-sm sm:flex-row sm:items-center sm:justify-between"><span className="font-semibold text-text">Current period result</span><span className="font-bold text-text">{formatINR(report.net_income)}</span></CardContent></Card></>}</div>;
}
